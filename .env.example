# Database Configuration
# Default PostgreSQL settings (change if needed)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=siva_medicals
DB_USER=postgres
DB_PASSWORD=password

# Session Configuration
# Generate a random string for production
SESSION_SECRET=siva-medicals-super-secret-key-change-in-production

# Server Configuration
PORT=3001
NODE_ENV=development

# Twilio WhatsApp Configuration (Optional)
# Get these from https://twilio.com/console
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_WHATSAPP_NUMBER=your_twilio_whatsapp_number